spring:
  application:
    name: audit-service
  config:
    import: "optional:configserver:http://config-server:9191"


server:
  port: 8085

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka/
